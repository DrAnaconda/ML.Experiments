namespace ML.Experiments.API.Enums
{
    // enum class RecordTypes(val code: Int) { Deep(2), LightOrOverall(1), Awake(3) }
    public enum SleepRecordType
    {
        LightOrOverall = 1, 
        Deep = 2,
        Awake = 3,
    }
}
